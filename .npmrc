# Supply-chain cooldown (requires npm >= 11.10.0).
# Refuse to install package versions published fewer than this many DAYS ago,
# so the ecosystem has time to detect a malicious/compromised release before it
# can enter our dependency tree. Governs `npm install` and `npm update` at
# resolution time — including AI-agent-driven installs in this repo.
#
# Not enforced by `npm ci` (it installs the exact versions already pinned in
# package-lock.json) and ignored by npm < 11.10.0. Automated PRs get an
# equivalent cooldown via .github/dependabot.yml.
#
# Escape hatch for an URGENT security fix newer than the window (npm has no
# per-package exclusion yet): override per command, e.g.
#   npm install next@latest --min-release-age=0
min-release-age=7

# Fail the install outright if the toolchain can't honor the setting above.
# package.json engines requires npm >= 11.10.0; without engine-strict, npm 10.x
# prints "Unknown project config" and installs with ZERO cooldown while the repo
# still claims to be protected. Silent degradation is worse than no control.
#
# Note this gates `npm install` and `npm ci`. Node 20 and 22 ship npm 10.x, so
# upgrade npm (`npm i -g npm@latest`) or use Node 24, which bundles npm 11.
# CI and the Vercel project are both pinned to Node 24 for this reason.
engine-strict=true

# Block dependency install scripts (preinstall/install/postinstall) unless the
# package is listed in package.json's `allowScripts`. Install scripts are the
# delivery mechanism for the Shai-Hulud / keyv worm family — the Aug 2026 keyv
# compromise worked by adding `"preinstall": "node setup.mjs"` to every affected
# package.
#
# Maintain the allowlist with `npm approve-scripts` (never `--all`); review each
# script by reading it. Entries are version-pinned, so a new version of an
# already-approved package re-enters review.
#
# Requires npm >= 11.16. Older npm prints `Unknown project config` and continues
# (verified: exit 0), so this degrades safely rather than breaking installs — but
# it also means it is NOT in force there. Vercel's builder currently ships npm
# 11.12.x, so treat this as enforced locally and in CI, advisory in production
# builds, until Vercel's image catches up. Verified enforcing on npm 11.17:
# removing an entry produces `ESTRICTALLOWSCRIPTS` and exit 1.
#
# Do NOT use `ignore-scripts=true` instead — that also disables OUR OWN scripts,
# which would silently skip `prebuild` and ship a stale sitemap.
strict-allow-scripts=true
